@model AppIndumentaria.Models.Indumentaria

@{
    ViewData["Title"] = "Delete";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-danger text-white">
                    <h3 class="mb-0">Eliminar Indumentaria</h3>
                </div>
                <div class="card-body">
                    <h5 class="text-danger">Está seguro de que desea eliminar esta indumentaria?</h5>
                    <dl class="row mt-3">
                        <dt class="col-sm-3">Nombre</dt>
                        <dd class="col-sm-9">@Model.Nombre</dd>

                        <dt class="col-sm-3">Talla</dt>
                       

                        <dt class="col-sm-3">Cantidad Disponible</dt>
                        
                    </dl>
                    <form asp-action="Delete" method="post">
                        <input type="hidden" asp-for="IndumentariaID" />
                        <input type="submit" value="Eliminar" class="btn btn-danger" />
                        <a asp-action="Index" class="btn btn-outline-secondary ms-2">Volver a la Lista</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
